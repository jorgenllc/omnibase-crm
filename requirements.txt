# OmniBase Market Intelligence Platform — backend dependencies
# Install:  python3 -m pip install -r requirements.txt
#           python3 -m playwright install chromium   (one-time, for crawl4ai)

crawl4ai>=0.3.0
python-dotenv>=1.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
